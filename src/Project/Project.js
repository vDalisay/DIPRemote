import React from 'react';

const Project = (props) =>{
    return(
        <div>
            <p>{props.name}</p>
            <p>{props.description}</p>

        </div>
    )
}

export default Project;